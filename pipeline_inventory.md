# Pipeline Inventory Report: NOAA Storm Events and PRISM Pipelines

**Inspection Date**: 2026-09-11  
**Project**: Paper 3 / NeuralCQR (County-level Weather & Extreme Storm Exposure)  
**Study Scope**: 676 Counties × 39 Years (1985–2023) = 26,364 County-Year Observations  
**Locked LOSO States**: Illinois (17), Indiana (18), Iowa (19), Minnesota (27), Missouri (29), Ohio (39)  
**Additional Dataset State**: Nebraska (31)  

---

## 1. NOAA Storm Events Pipeline (`data/noaa_storm_pipeline/`)

| File Path | File Type | Purpose | Current Status | Needs Modification? | Details & Modifications Needed |
|:---|:---|:---|:---|:---|:---|
| `data/noaa_storm_pipeline/README_NOAA_Storm_Events.md` | Markdown Documentation | Documents pipeline architecture, historical taxonomy break (1996), and execution steps. | Active documentation, but assumed sandbox network isolation (which is now resolved; live NCEI connection confirmed HTTP 200). | **Yes** (Update) | Update network status, execution instructions, and document zone-county crosswalk resolution. |
| `data/noaa_storm_pipeline/requirements.txt` | Python Dependencies | Specifies required libraries (`pyyaml`, `requests`, `pandas`, `pyarrow`, `python-dateutil`). | Valid. Core libraries installed and verified in Python 3.13. | **No** | Dependencies already met in environment. |
| `data/noaa_storm_pipeline/config/storm_config.yaml` | YAML Configuration | Central configuration for NCEI URLs, study period, directory paths, taxonomy mappings, damage multipliers. | Points paths to `data/noaa_storm_events/` instead of required `data/noaa_storm_pipeline/` structure; references external crosswalk file. | **Yes** (Update) | Update output/raw/processed/manifest paths to `data/noaa_storm_pipeline/{raw,processed,manifests,logs,checkpoints}`; set official NWS zone crosswalk source. |
| `code/pipelines/noaa_storm/storm_common.py` | Python Script | Shared helper functions: `load_config`, `resolve_path`, `setup_logger`, `ManifestWriter`. | Valid, but path resolution anchors to parent of config rather than cleanly supporting configurable project root. | **Yes** (Update) | Ensure robust `Path(__file__).resolve().parents[...]` project root anchoring so all paths align with `data/noaa_storm_pipeline/`. |
| `code/pipelines/noaa_storm/download_storm_events.py` | Python Script | Scrapes NOAA NCEI directory listing and downloads yearly `StormEvents_details-ftp_v1.0_d<YEAR>_c<DATE>.csv.gz` files. | Logic is sound and verified against live NCEI index (77 files found; all 39 study years 1985–2023 available). | **Yes** (Minor update) | Ensure paths write to `data/noaa_storm_pipeline/raw/`; ensure manifest logging matches required format. |
| `code/pipelines/noaa_storm/inspect_storm_events.py` | Python Script | Inspects downloaded yearly storm files, reports column schemas and distributions. | Functioning, but assumed lowercase column names whereas live NCEI files have uppercase headers (`EVENT_ID`, `CZ_TYPE`, `BEGIN_DATE_TIME`). | **Yes** (Update) | Support case-insensitive column matching so both uppercase and lowercase source schemas parse cleanly. |
| `code/pipelines/noaa_storm/clean_storm_events.py` | Python Script | Cleans raw yearly files, parses damage strings (`10.00K`, `1.5M`), constructs county GEOID. | Leaves Zone-type (`CZ_TYPE == 'Z'`) events with `GEOID = NaN` because crosswalk was missing; leaves `event_duration_hours` uncomputed (`None`). | **Yes** (Major update) | 1. Integrate authoritative NWS public zone-to-county crosswalk (`bp18mr25.dbx`).<br>2. Compute `event_duration_hours` from `BEGIN_DATE_TIME` and `END_DATE_TIME`.<br>3. Map tornado scales (`F0-F5`, `EF0-EF5`) to `tornado_scale_numeric` (0–5).<br>4. Mark unresolved zones as `GEOID = NA`, `geography_status = UNRESOLVED`. |
| `code/pipelines/noaa_storm/aggregate_storm_county_year.py` | Python Script | Aggregates cleaned storm events into county-year features. | Only groups rows with reported events; does **NOT** build the mandatory 676 × 39 = 26,364 county-year skeleton; sorts tornado scales lexicographically. | **Yes** (Major update) | 1. Build complete 676 × 39 skeleton from `Paper3_MegaDataset_SPEI_FINAL.csv`.<br>2. Left-join events onto skeleton.<br>3. Implement explicit `storm_data_status` (`VALID_ZERO`, `REPORTED_EVENTS`, etc.).<br>4. Aggregate duration (`total_storm_event_hours`, `maximum_event_duration_hours`), accounting for `EPISODE_ID`.<br>5. Compute proper numeric tornado scales and magnitudes. |
| `code/pipelines/noaa_storm/validate_storm_events.py` | Python Script | Produces quality and coverage reports (`noaa_storm_quality_report.csv`, `storm_reporting_coverage_report.csv`). | Valid logic for event-level QC, but needs to validate the full 26,364 county-year skeleton. | **Yes** (Update) | Add verification for exact 26,364 rows, missingness checks, and county coverage across all 7 states. |
| `code/pipelines/noaa_storm/merge_with_mega_dataset.py` | Python Script | Merges county-year storm features into the MegaDataset to create `Paper3_NOAA_Storm_Enhanced.csv`. | References old file path `Paper3_MegaDataset_SPEI_FINAL(1).csv`. | **Yes** (Update) | Point to authoritative `Paper3_MegaDataset_SPEI_FINAL.csv` and output to `data/noaa_storm_pipeline/processed/Paper3_NOAA_Storm_Enhanced.csv`. |
| `data/noaa_storm_pipeline/data/noaa_storm_events/*` | Directory Tree | Subfolders: `checkpoints/`, `extracted/`, `logs/`, `manifests/`, `processed/`, `raw/`. | Currently contains only `.gitkeep` files; 0 raw data files present. | **Yes** (Restructure) | Relocate/align directly under `data/noaa_storm_pipeline/{raw,processed,manifests,logs,checkpoints}` per Part G. |
| `data/noaa_storm_pipeline/inputs/` | Directory | Holds external inputs (e.g. crosswalks). | Contains `.gitkeep`. | **Yes** (Populate) | Download and cache official NWS zone-county crosswalk (`bp18mr25.dbx`). |

---

## 2. PRISM Weather Pipeline (`data/prism_pipeline/`)

| File Path | File Type | Purpose | Current Status | Needs Modification? | Details & Modifications Needed |
|:---|:---|:---|:---|:---|:---|
| `data/prism_pipeline/README_PRISM.md` | Markdown Documentation | Documents PRISM downloader architecture, network constraints, execution order. | Documents sandbox network isolation (now verified reachable: `services.nacse.org` returns HTTP 200). Assumes PRISM internal raster format is `.bil`. | **Yes** (Update) | Document verified GeoTIFF format (`.tif`), EPSG:4269 CRS, and `exactextract` fractional weighting implementation. |
| `data/prism_pipeline/requirements.txt` | Python Dependencies | Lists dependencies (`rasterio`, `geopandas`, `shapely`, `rasterstats`, etc.). | `exactextract` was missing; `exactextract-0.3.0` has now been installed and verified in the environment. | **Yes** (Update) | Add `exactextract>=0.3.0`. |
| `data/prism_pipeline/config/prism_config.yaml` | YAML Configuration | Central configuration for PRISM endpoint, study period, paths, variables (`tmax`, `tmin`, `ppt`), validation bounds. | Points paths to `data/prism/` rather than `data/prism_pipeline/`; references external TIGER shapefile path that was not populated. | **Yes** (Update) | 1. Update paths to `data/prism_pipeline/{raw,processed,county_daily,manifests,logs,checkpoints}`.<br>2. Point `county_boundary_file` to local cached GeoJSON: `outputs (1)/geo_cache/counties_fips.json` (contains all 676 study counties with verified EPSG:4326 geometries). |
| `code/pipelines/prism/prism_common.py` | Python Script | Shared helper functions: `load_config`, `resolve_path`, `setup_logger`, `daterange`, `ManifestWriter`. | Valid, but path resolution anchors to parent of config rather than root. | **Yes** (Update) | Ensure robust project-root anchoring using `Path(__file__).resolve().parents[...]`. |
| `code/pipelines/prism/download_prism.py` | Python Script | Resumable downloader for PRISM daily CONUS 4km grids. | Valid, resumable, skip-on-success. Tested live with HTTP 200. | **Yes** (Minor update) | Ensure download path aligns with `data/prism_pipeline/raw/{tmax,tmin,ppt}/`. |
| `code/pipelines/prism/inspect_prism.py` | Python Script | Inspects downloaded PRISM archive, extracts metadata (CRS, resolution, bounds, nodata). | Assumed `.bil` file inside zip; live inspection confirms PRISM delivers GeoTIFF (`.tif`). | **Yes** (Update) | Add direct `.tif` inspection, output metadata to `data/prism_pipeline/processed/prism_grid_metadata.csv`. |
| `code/pipelines/prism/aggregate_prism_county.py` | Python Script | Zonal aggregation of daily PRISM rasters to county boundaries. | 1. Looked only for `.bil` files inside zip (failed on `.tif`).<br>2. Used `rasterstats` with `all_touched=False` (pixel-center heuristic, not true fractional-area weighting). | **Yes** (Major update) | 1. Handle `.tif` rasters.<br>2. Replace heuristic with `exactextract` for true fractional-area weighting.<br>3. Reproject county geometries to match raster CRS (EPSG:4269).<br>4. Output to `data/prism_pipeline/processed/prism_county_daily_1985_2023.parquet`. |
| `code/pipelines/prism/validate_prism.py` | Python Script | Download-level QC, county-daily coverage QC, plausibility bounds QC. | Functional, but paths point to `data/prism/manifests/`. | **Yes** (Update) | Align paths to `data/prism_pipeline/processed/` and `data/prism_pipeline/manifests/`. |
| `data/prism_pipeline/data/prism/*` | Directory Tree | Subfolders: `checkpoints/`, `county_daily/`, `county_year/`, `logs/`, `manifests/`, `processed/`, `raw/{ppt,tmax,tmin}`. | Initial state had only `.gitkeep` files; 0 raw files present initially. Tested single test downloads (1985-07-15 and 2019-07-15). | **Yes** (Restructure) | Align under `data/prism_pipeline/{raw,processed,manifests,logs,checkpoints}` per Part G. |
| `data/prism_pipeline/inputs/` | Directory | Reserved for external input files. | Currently `.gitkeep`. | **No** | County geometry will use authoritative `outputs (1)/geo_cache/counties_fips.json`. |

---

## 3. Pre-existing Data & Cross-Validation Sources

| File Path | Description | Relevance to Pipeline Task |
|:---|:---|:---|
| `Paper3_MegaDataset_SPEI_FINAL.csv` | Master project dataset: 26,364 rows (676 counties × 39 years, 1985–2023). Contains annual weather, CDHW, yields, soil, and land cover. | **Authoritative skeleton reference** for county GEOIDs, names, states, and years. Also source for ERA5 cross-validation in Part D. |
| `outputs (1)/geo_cache/counties_fips.json` | Complete GeoJSON containing 3,221 US counties (WGS84 EPSG:4326). | **Authoritative spatial geometry**: Exactly matches all 676 study counties (100% match verified). Used for PRISM fractional-area extraction. |
| `outputs/noaa_weather/noaa_daily_county_weather.csv` | Daily county weather dataset (8.3M rows, 1985–2023) from NOAA CDO GHCND stations (`TMAX_degC`, `TMIN_degC`, `PRCP_mm`). | Used exclusively in **Part D Combined Validation** to evaluate PRISM against station-based NOAA weather. |
